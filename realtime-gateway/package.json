{
  "name": "wir-player-realtime-gateway",
  "version": "0.1.0",
  "description": "Socket.IO outbox gateway for WirGroup CMS Players",
  "main": "src/index.cjs",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "node src/index.cjs",
    "test": "node --test test/*.test.cjs",
    "test:watch": "node --test --watch test/*.test.cjs"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "pg": "^8.16.3",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
